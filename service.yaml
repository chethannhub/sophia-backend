apiVersion: v1
kind: Service
metadata:
  name: news-teller-service
  labels:
    app: news-teller
spec:
  type: LoadBalancer
  selector:
    app: news-teller
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    name: http
  sessionAffinity: None
